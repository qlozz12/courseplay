;(function ($) { $(document).ready(function () {
$('body').removeClass('no-js').addClass('js');

/*
$('#navSidebar > ul').slicknav({
	easingOpen: 'linear',
	easingClose: 'linear',
	allowParentLinks: true
});
*/




/**
function startsWith(str, startStr) {
	return str.substr(0, startStr.length) == startStr;
}

var currentId = '#intro';
var hash = window.location.hash;
if (hash && hash !== 'undefined') {
	currentId = hash.substr(1, hash.length - 1); // omit the hash sign
	// currentId = hash;
};
// console.log(currentId, hash)

if (startsWith(currentId, 'hud')) {
	console.log('startsWith hud')
	// TODO
} else if (startsWith(currentId, 'driveMode')) {
	console.log('startsWith driveMode')
	// TODO
};
**/


}); })(jQuery); //END document.ready
